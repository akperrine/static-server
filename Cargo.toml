[package]
name = "static_server"
version = "0.1.0"
edition = "2021"


[dependencies]
clap = { version = "4.4.6", features = ["cargo"] }
hyper = {version = "0.14", features = ["full"]}
http = "*"
tokio = {version = "*", features = ["full"]}
